#include <iostream>
#include "../include/Matrix.hpp"

using namespace std; // memcpy

template <typename T>
Matrix<T>::Matrix(size_t row, size_t column, size_t channel)
{
    cout << "Constructer!" << endl;
}
